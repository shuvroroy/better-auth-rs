[package]
name = "better-auth"
description = "The most comprehensive authentication framework for Rust"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[lints]
workspace = true

[workspace]
resolver = "2"
members = ["crates/core", "crates/types", "crates/api", "crates/derive", "crates/seaorm", "crates/seaorm-macros", "crates/schema-registry", "crates/cli"]
exclude = ["examples/sqlx-custom-entities", "examples/fullstack/backend", "compat-tests/rust-server"]

[workspace.package]
version = "1.0.0-alpha.2"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/better-auth-rs/better-auth-rs"

[workspace.dependencies]
# Sub-crates
better-auth-types = { version = "1.0.0-alpha.2", path = "crates/types" }
better-auth-core = { version = "1.0.0-alpha.2", path = "crates/core" }
better-auth-api = { version = "1.0.0-alpha.2", path = "crates/api", default-features = false }
better-auth-macros = { version = "1.0.0-alpha.2", path = "crates/derive", package = "better-auth-macros" }
better-auth-seaorm = { version = "1.0.0-alpha.2", path = "crates/seaorm" }
better-auth-schema-registry = { version = "1.0.0-alpha.2", path = "crates/schema-registry" }
prettyplease = "0.2"

# Asynchronous runtime
tokio = { version = "1.0", features = ["full"] }
async-trait = "0.1"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Logging and tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Error handling
thiserror = "2.0"

# Time handling
chrono = { version = "0.4", features = ["serde"] }

# Cookie handling
cookie = "0.18"

# Data structures
uuid = { version = "1.18", features = ["v4", "v7", "serde"] }

# Validation
validator = { version = "0.19", features = ["derive"] }

# Crypto
argon2 = "0.5"
rand = "0.8"
base64 = "0.22"
jsonwebtoken = "9"
sha2 = "0.10"
aes-gcm = "0.10"
hkdf = "0.12"
hmac = "0.12"
totp-rs = { version = "5", features = ["gen_secret", "otpauth"] }

# HTTP client
reqwest = { version = "0.12", default-features = false, features = ["json"] }

# Web frameworks
axum = "0.8"
tower = "0.5"
url = "2"

# Cache
redis = "0.27"

# Proc macro utilities
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }

[features]
default = ["native-tls"]
native-tls = ["better-auth-api/native-tls"]
rustls = ["better-auth-api/rustls"]
axum = [
    "dep:axum",
    "dep:tower",
    "dep:url",
    "dep:http-body-util",
    "better-auth-core/axum",
    "better-auth-api/axum",
]
seaorm2 = ["dep:better-auth-seaorm"]
redis-cache = ["dep:redis"]

[dependencies]
# Core crates
better-auth-core = { workspace = true }
better-auth-api = { workspace = true }
better-auth-seaorm = { workspace = true, optional = true }

# Asynchronous runtime
tokio = { workspace = true }
async-trait = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = "0.9"
clap = { version = "4.5", features = ["derive"] }

# Error handling
thiserror = { workspace = true }

# Time handling
chrono = { workspace = true }

# Data structures
uuid = { workspace = true }

# Crypto
argon2 = { workspace = true }
rand = { workspace = true }
base64 = { workspace = true }
jsonwebtoken = { workspace = true }

# Web framework (optional)
axum = { workspace = true, optional = true }
tower = { workspace = true, optional = true }
url = { workspace = true, optional = true }

# Cache (optional)
redis = { workspace = true, optional = true }
tracing.workspace = true
http-body-util = { version = "0.1", optional = true }

[dev-dependencies]
better-auth-seaorm = { workspace = true }
tokio = { workspace = true, features = ["full", "test-util"] }
serde_yaml = "0.9"
tower-http = { version = "0.6", features = ["cors"] }
tracing-subscriber = { workspace = true }
oas3 = { version = "0.20.1", features = ["yaml-spec"] }
proptest = "1.10.0"
reqwest = { workspace = true }

[[example]]
name = "axum_server"
required-features = ["axum", "seaorm2"]

[[example]]
name = "postgres_usage"
required-features = ["seaorm2"]

[[bin]]
name = "sync_better_auth_openapi"
path = "scripts/sync_better_auth_openapi.rs"

[[bin]]
name = "openapi_alignment_report"
path = "scripts/openapi_alignment_report.rs"

[[bin]]
name = "generate_docs_openapi"
path = "scripts/generate_docs_openapi.rs"
required-features = ["seaorm2"]

[workspace.lints.clippy]
allow_attributes = "warn"
allow_attributes_without_reason = "warn"
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"
todo = "warn"
unimplemented = "warn"
unreachable = "warn"
indexing_slicing = "warn"
panic_in_result_fn = "warn"
undocumented_unsafe_blocks = "warn"
missing_safety_doc = "warn"
await_holding_lock = "warn"
await_holding_refcell_ref = "warn"
unused_async = "warn"
multiple_unsafe_ops_per_block = "warn"
unnecessary_safety_comment = "warn"

[workspace.lints.rust]
unfulfilled_lint_expectations = "warn"
unreachable_pub = "warn"
unused_results = "warn"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
incremental = false
debug = false

[profile.ci]
inherits = "release"
opt-level = 2
lto = "thin"
codegen-units = 16
